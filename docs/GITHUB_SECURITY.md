# GitHub security settings

Before publication, enable secret scanning and push protection, private vulnerability reporting, Dependabot alerts and security updates, and read-only default workflow-token permissions.

Protect `main`: require pull requests and approvals; require CI, CodeQL, dependency review, and secret-scan checks; require branches to be current; disable force pushes and deletion. Restrict bypasses to repository administrators and review them periodically.
